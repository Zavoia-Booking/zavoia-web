import {
  localeBlockContent,
  localeSlug,
  localeString,
  localeText,
} from "./locale";
import { post } from "./post";

export const schemaTypes = [
  localeString,
  localeText,
  localeSlug,
  localeBlockContent,
  post,
];
